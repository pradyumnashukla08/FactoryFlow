# Frontend environment variables (CRA convention: REACT_APP_ prefix)
# In development, the CRA proxy (package.json) handles API routing.
# In production, set this to the full backend URL.
REACT_APP_API_URL=/api
